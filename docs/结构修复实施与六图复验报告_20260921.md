# 结构修复实施与六图复验报告

日期：2026-09-21。范围：执行《结构失真深层根因与最小修复方案》，只到生图。

## 1. 实施范围

本轮以开始实施时的工作区为基线，不把之前尚未提交的修改重复计入。

| 生产文件 | 新增/删除行 | 本轮修改 |
|---|---:|---|
| `core/visual_design_kit_compiler.py` | +2 / -1 | func、size 即使没有销售归属争议，也进入已有 product_depiction 结构复核 |
| `core/visual_semantics.py` | +14 / -5 | 原观察和复核明确所属隔间、安装面、连接对象、互斥状态；声明本身不能替代像素证据 |
| `core/visual_design_kit.py` | +7 / -3 | 原规划保留已证实的部件关系和视角；替换含混的替代位置示意要求 |
| `core/image_prompt_compiler.py` | +6 / -2 | 在原 func/size 编译出口投影互斥状态和安装关系，避免把示意状态画成同时存在的实体 |

生产文件：新增 0、删除 0、修改 4；合计约 **+29 / -11 行**。没有新增调度器、流程阶段、供应商路由、自动重试、QA 或发布规则。

物理删除/替换的旧行为：普通 func/size 在无销售争议时绕过结构复核的条件；关于结构关系、示意状态的旧含混提示；测试中把空 design_review 当作正确结果的断言。没有保留旧分支作为兼容回退。

既有测试文件修改 5 个（合计 +59 / -20 行），没有新增测试模块或用例数量：`failure_boundary_regression_fixture.py`、`remediation_recheck_fixture.py`、`test_image_branch_v1.py`、`test_visual_design_remediation.py`、`visual_recovery_regression_fixture.py`。

## 2. 已完成的代码验证

定向命令：

```powershell
D:\anaconda\python.exe -m unittest tests.test_image_branch_v1 tests.test_visual_design_remediation tests.test_root_cause_remediation tests.test_generation_state_contract
```

结果：**23 项通过，8.380 秒**。

默认生产套件（收尾运行一次）：

```powershell
D:\anaconda\python.exe scripts/run_production_tests.py
```

结果：**100 项通过，11.972 秒**。`git diff --check` 通过。

覆盖真实行为：没有销售归属争议仍发送结构复核；空复核不得冒充成功；源事实错误回既有纠错出口；错误展示计划仅修对应 brief；当前复核可以缓存复用；中断恢复不重复已完成复核；最终提示词包含互斥/安装关系。

## 3. 实际观察和规划复核

工作目录：`test_runs/freeze_4asin_2var_20260920/B0GZL7J2VT_20260920T095006145270`。

本次调用原 classify、brief 阶段。两个 child 各有 5 个实际 product_depiction 复核项（4 个 func + 1 个 size），合计 10 个；此前同类计划没有实际 design_binding 请求。

初次新观察已纠正“脚底圆环是塑料垫”的错误和磁吸扣所属隔间。但两张防倾倒源图仍只写“后上沿”，实际复核也放过了这类含混表述。经人工逐图检查，使用原 `review-source-role --role reobserve` 对原木 source 6、白柜 source 8 重新观察，再走原 classify、brief。只有这两张的来源观察内容发生变化，两个 child 的计划随当前性变化重新生成。不能把这次结果描述成首次自动观察完全正确。

最终事实明确竖直后部安装面、长孔臂与柜体固定关系、垂直折弯臂与墙面关系，并投影到原计划和最终提示词。没有修改图像基础尺寸，实际请求仍为 1024×1024，文件经过既有放大流程保存。

## 4. 六角色实图复验

逐图结果与实际提示词：见 [六图复验页面](../test_runs/freeze_4asin_2var_20260920/audit/structure_fix_20260921/六图复验.html)。保留上一轮、当轮首张及修订候选，不能用旧版本图片充当本轮成功结果。

当前 **4/6 个角色取得新候选，含 2 次局部修订共保存 6 张候选文件**。模型复核返回 supported 只证明本次复核链被调用，不等于图片质量合格。

| child / role | 实图检查结果 |
|---|---|
| 原木 `B0GZKX7DRZ / func` | 首发 524；经用户明确授权的一次重发仍 524。没有本轮图片，层板实体化修复尚未验证 |
| 原木 `func_03` | 磁吸扣回到下柜门框，脚底夸张圆环消失。但磁吸扣放大图引线指向柜底；一次局部修订后仍错误，未通过 |
| 原木 `func_02` | 主图和放大图的长孔臂保留在竖直面，没有变成柜顶面安装；本项可见安装关系改善 |
| 白柜 `B0GZL7J2VT / func_04` | 放大图竖直安装面恢复。首张仍有错误柜顶引线；局部修订去掉引线和圆点，但残留小灰痕，右门内侧条纹被抹平。未通过 |
| 白柜 `func_02` | 524、远端结果未知。已单独请求一次重发授权，未收到授权前不重发；本轮层板对照未完成 |
| 白柜 `func` | 双门全开、开放格与下柜两层容量分区保持；该正常对照未见上述退化 |

本轮共提交 9 次图像请求：6 次首发、1 次明确授权重发、2 次原入口 targeted_edit；6 次取得候选，3 次 524 未取得结果。不得将远端未知当作确认未生成。原木层板的一次重发授权已消耗，未继续第三次请求；白柜层板没有擅自使用原木图的授权。

已核对这 6 张文件的图片 SHA、实际请求提示词 SHA、基础任务提示词 SHA，均与各自当前记录一致；两次 targeted_edit 的父候选 SHA 正确。修订入口会加入请求说明，因此基础提示词和实际请求提示词的 SHA 不应直接要求相等。逐图页面的 22 个本地图片/提示词链接均存在。证据见 `verification.json`；没有用指纹正确推断视觉正确。

## 4.1 仍未解决的深层限制

1. **调用覆盖已补齐，复核判断仍可能漏检。** 初次模型复核放过了安装面含混描述；这次需要人工点名两张源图再观察。不能宣称语义提示保证自动识别全部关系错误。
2. **正确合同不保证图像执行正确。** 磁吸扣实际安装位置改善，但说明引线关联仍错；局部修订的实际请求包含明确修正指令，输出仍未执行。
3. **局部编辑仍可能重绘无关区域。** 白柜修订已经要求保持其他结构、文字和布景，但门内侧纹理仍丢失。首张及修订图都保留，未将最新版本自动视为更好。
4. **层板问题缺本轮实图证据。** 两个层板角色都遇到供应商超时，不能据提示词、测试或规划复核宣布实体化问题解决。

本轮不因上述限制继续扩大代码、增加新裁剪/蒙版流程或增加自动重试。结论为：**最小代码修改及离线验证完成，实际结构有改善，六图验证尚未全部完成，图片质量未全部通过。**

## 5. 边界与留存

本轮只生成约定的 6 个角色；当前性改变涉及的其余 12 个角色未在新版本重新生图，不计入本次通过数量。没有执行 QA、发布或模板填充。

审计证据保存在 `test_runs/freeze_4asin_2var_20260920/audit/structure_fix_20260921/`，包括实施前代码副本、精确行数差异、测试日志、观察/规划前后记录、逐次生成日志、提示词和候选绑定。
