# 冻结测试：4 个 ASIN、每个 2 个变体（2026-09-21）

## 结果

本次重新选择了 4 个品类，并为每个品类固定 2 个变体。抓取、下载、分类、视觉规划和生图均已执行；QA、发布和模板阶段按用户要求未启动。

| 品类 | 变体 1 | 变体 2 | 候选图数量 | 结果 |
|---|---|---|---:|---|
| 浴室柜 `bathroom_cabinet` | `B0H1VSH5GM`（Natural） | `B0H1VZX3WZ`（White） | 18（各 9） | 生图完成 |
| 床架 `bed_frame` | `B0DSSKTXXW`（Natural） | `B0DSRTM3HX`（White） | 20（各 10） | 生图完成 |
| 人造树 `artificial_tree` | `B0GG3DF88S` | `B0GG3TVW6R` | 19（10 + 9） | 生图完成 |
| 浴室镜柜 `medicine_cabinet` | `B0GF2357FX` | `B0DCBRXCMG` | 18（各 9） | 生图完成 |

合计 **8 个变体、75 张候选图**。所有已完成变体的 `planned_image_completion_status=success`、`planned_image_unresolved_count=0`；QA 记录全部为 `not_run`。

## 模型与回执

- `func*` 和 `size` 的最终候选均使用 `gpt-image-2.5-sunburst`（QC-YC 或 CXK 路由）。
- `main`、`scene*` 按当前 2.5 模型池路由；失败时沿现有候补链切换到可用模型。
- 汇总请求数 79，候选提交 75；其中 9 次为已有候选复用，保留在各 Job 的结果回执中。
- 浴室柜出现 HTTP 524 未知结果，按已授权的未知结果重发策略完成有界重发并落盘；没有把未知结果直接当作失败丢弃。
- 第二床架变体出现上游 HTTP 500 未知结果和一次无效画布内容错误，按现有路由切换后完成；未添加新的恢复流程。

## 运行过程中的范围与容量问题

首次并发启动多个 Job 时，本机图像资源容量触发了 retryable/capacity requeue，未计入有效候选。随后改为现有流程的单 Job、`workers=1` 顺序恢复，所有 8 个变体均完成生图。

抓取范围曾正确包含两个变体，但部分首 Job 的 brief 任务清单只展开了首变体。为保持冻结范围不变，没有修改生产代码；对缺失变体建立了同一流程的独立 Job，并使用同一 `fetch → download → classify → brief → generate` 链路补齐。第二浴室镜柜变体的两个 brief 首次失败后，在同一 Job 内重跑 brief→generate，最终补齐到 9/9。

## 证据位置

- 测试根目录：[test_runs/freeze_4asin_2var_20260921](D:/Amazon_pics/amazon_listing_factory/test_runs/freeze_4asin_2var_20260921)
- 浴室柜（两个变体同 Job）：[B0H1VSH5GM_20260921T064708635657](D:/Amazon_pics/amazon_listing_factory/test_runs/freeze_4asin_2var_20260921/B0H1VSH5GM_20260921T064708635657)
- 床架变体：[B0DSSKTXXW_20260921T064708634658](D:/Amazon_pics/amazon_listing_factory/test_runs/freeze_4asin_2var_20260921/B0DSSKTXXW_20260921T064708634658)、[B0DSRTM3HX_20260921T073131505348](D:/Amazon_pics/amazon_listing_factory/test_runs/freeze_4asin_2var_20260921/B0DSRTM3HX_20260921T073131505348)
- 人造树变体：[B0GG3DF88S_20260921T064959445715](D:/Amazon_pics/amazon_listing_factory/test_runs/freeze_4asin_2var_20260921/B0GG3DF88S_20260921T064959445715)、[B0GG3TVW6R_20260921T073131505348](D:/Amazon_pics/amazon_listing_factory/test_runs/freeze_4asin_2var_20260921/B0GG3TVW6R_20260921T073131505348)
- 浴室镜柜变体：[B0GF2357FX_20260921T064959444715](D:/Amazon_pics/amazon_listing_factory/test_runs/freeze_4asin_2var_20260921/B0GF2357FX_20260921T064959444715)、[B0DCBRXCMG_20260921T073131509582](D:/Amazon_pics/amazon_listing_factory/test_runs/freeze_4asin_2var_20260921/B0DCBRXCMG_20260921T073131509582)

每个 Job 的 `reports/production_summary_v3.json`、`reports/imagegen_results_v3.json`、`reports/progress_v1.jsonl` 和 `images/generated/<ASIN>/` 是本次冻结证据。此次只核对了任务状态、候选数量、路由、模型和回执；未做逐图视觉质量验收，因此“生图完成”不等于“QA 通过”。

## 代码与测试范围

本次仅运行测试 Job 和新增本报告，没有修改生产代码、没有删除运行路径或测试。此前模型池修改已通过既定定向测试和生产套件；本次冻结运行未重复执行代码测试，原因是本轮没有生产代码变更。
